{% from '../components/button.njk' import Button %}

<section class="wizard" role="dialog" aria-hidden="true" role="dialog" aria-modal="true" aria-labelledby="wizard-title" aria-describedby="wizard-description">
    <div class="wizard-mask" aria-hidden="true"></div>
    <div class="wizard-focus" aria-hidden="true"></div>
    <div class="wizard_tooltip" role="document">
        <div class="wizard_tooltip-body">
            <header class="heading">
                <h3 id="wizard-title" class="title">{{ copy.wizard.title[lang] }}</h3>
                <p id="wizard-steps" class="steps" aria-label="{{ copy.wizard.steps.aria[lang] }}">1/4</p>
            </header>
            <p id="wizard-description" class="description">{{ copy.wizard.content.toolbar[lang] }}</p>
            <!-- Add live region for step announcements -->
            <div aria-live="polite" aria-atomic="true" class="sr-only" id="wizard-announcements"></div>
        </div>
        <div class="wizard_tooltip-foot">
            {{ Button(copy.wizard.button_group.skip, 'button secondary small') }}
            <div class="button_group" role="group" aria-label="{{ copy.wizard.button_group.aria[lang] }}">
                {{ Button(copy.wizard.button_group.previous, 'button ghost small', 'chevron-left') }}
                {{ Button(copy.wizard.button_group.next, 'button primary small', '', 'chevron-right') }}
            </div>
        </div>
    </div>
</section>